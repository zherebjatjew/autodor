<table class="profile" summary="Profile information">
  <tr>
    <td class="main">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </td>
    <td class="sidebar round">
      <%= link_to "Редактировать профиль", edit_user_path(current_user) %><br />
      <strong>График работы: </strong><%= @user.schedule %><br />
      <%= pluralize @user.orders.count, "заявка" %>
    </td>
  </tr>
</table>
  <% unless @user.orders.empty? %>
    <table class="orders" summary="Мои заявки">
      <%= render @orders %>
    </table>
    <%= will_paginate @orders %>
  <% end %>
  <%= link_to "добавить заявку", new_order_path(@user) %>

