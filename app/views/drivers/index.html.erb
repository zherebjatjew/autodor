<script type="text/javascript">
  function toggleExpansion(button) {
      button.text(button.text() == '▼' ? '▲' : '▼');
      driver_id = button.attr('id').substring(6);
      container = $j('#form' + driver_id);
      container.slideToggle(150);
  }

  $j(document).ready(function() {
    $j('a[data-remote=false]').live('click', function(event) {
      event.preventDefault();
      toggleExpansion($j(this));
      return false;
    });
  
    $j('a[data-remote=false]').live('ajax:before', function(event) {
      // Decpite we have turned 'data-remote' off, rails.js is still
      // executing ajax request on click, so we are to cancel it:
      return false;
    });

    $j('a[data-remote=true]').live('ajax:complete', function(event, xhr, status) {
      button = $j(this);
      event.preventDefault();
      button.attr('data-remote', false);
      driver_id = button.attr('id').substring(6);
      container = $j('#form' + driver_id);
      container.html(xhr.responseText);
      if (container.find('td').length == 0) {
        // To have just something within the table
        container.find('table').append('<tr><td>Нет ни одного заказа</tr>');
      }
      toggleExpansion(button);
      return false;
    });
  });
</script>

<h2>Наши водители</h2>

<%= link_to "Добавить", new_driver_path, :class => 'command' %>

<div class="clients">
  <%= will_paginate %>
  <%= render @drivers %>
  <%= will_paginate %>
</div>

<%= link_to "Добавить", new_driver_path, :class => 'command' %>

