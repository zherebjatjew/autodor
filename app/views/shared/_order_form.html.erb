<%= form_for @order do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :info %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>
